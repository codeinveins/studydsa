package com.mixer.problems.linkedlists;

/*
Reverse the input linked list

Solution :
        Approach 1: point head to last element and null to first element.next
            Node prev = null;
            Node temp = head;
            Node front = temp.next;
            temp.next = prev;
            front.next = temp;


Ques: Can you reverse linked list without using iterations (It means using recursion)
Solution:
        Approach :
 */
public class ReverseLinkedList {
}
